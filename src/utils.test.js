import { linearScale } from './utils';

describe('Utils', () => {
  describe('linearScale()', () => {
    it('should work', () => {
      // TODO write a more pertinent test

      expect(linearScale()).not.toBeNull();
    });
  });
});
